/**
 * Módulo Menú con subproductos.
 * Misma estructura de estilos que admin-menu-compuesto-crear.
 * Aquí se implementará el flujo para crear menús con subproductos.
 */
document.addEventListener("DOMContentLoaded", () => {
    // Inicialización futura: selector de producto base, lista de subproductos, etc.
});
